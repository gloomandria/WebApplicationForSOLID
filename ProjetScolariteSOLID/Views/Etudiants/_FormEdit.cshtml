@model ProjetScolariteSOLID.ViewModels.EtudiantsViewModel

<input type="hidden" name="Id" value="@Model.Etudiant.Id" />
<input type="hidden" name="NumeroEtudiant" value="@Model.Etudiant.NumeroEtudiant" />
<input type="hidden" name="DateInscription" value="@Model.Etudiant.DateInscription.ToString("yyyy-MM-dd")" />

<div class="row g-3">
    <div class="col-md-4">
        <label class="form-label">N° Étudiant</label>
        <input value="@Model.Etudiant.NumeroEtudiant" class="form-control" readonly />
    </div>
    <div class="col-md-4">
        <label class="form-label">Nom</label>
        <input name="Nom" value="@Model.Etudiant.Nom" class="form-control" required />
    </div>
    <div class="col-md-4">
        <label class="form-label">Prénom</label>
        <input name="Prenom" value="@Model.Etudiant.Prenom" class="form-control" required />
    </div>
    <div class="col-md-8">
        <label class="form-label">Email</label>
        <input name="Email" value="@Model.Etudiant.Email" class="form-control" type="email" required />
    </div>
    <div class="col-md-4">
        <label class="form-label">Téléphone</label>
        <input name="Telephone" value="@Model.Etudiant.Telephone" class="form-control" />
    </div>
    <div class="col-md-6">
        <label class="form-label">Date de naissance</label>
        <input name="DateNaissance" value="@Model.Etudiant.DateNaissance.ToString("yyyy-MM-dd")" class="form-control" type="date" required />
    </div>
    <div class="col-12">
        <label class="form-label">Adresse</label>
        <input name="Adresse" value="@Model.Etudiant.Adresse" class="form-control" />
    </div>
</div>
